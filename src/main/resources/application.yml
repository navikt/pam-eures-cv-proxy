micronaut:
  application:
    name: proxy
  server:
    context-path: /pam-eures-cv-eksport
    port: 9031


pam-eures-cv-eksport:
  scheme: http
  host: ${PROXIED_HOST:localhost}
  port: ${PROXIED_PORT:9030}